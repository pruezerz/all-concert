# 📦 คู่มือการติดตั้ง All Concert System

## สิ่งที่ต้องมีก่อนติดตั้ง (Prerequisites)

### 1. ✅ Java Development Kit (JDK)
**ต้องมี JDK เวอร์ชัน 17 หรือสูงกว่า**

#### ตรวจสอบว่าติดตั้งแล้วหรือยัง
เปิด Command Prompt หรือ PowerShell แล้วพิมพ์:
```bash
java -version
javac -version
```

**ถ้าเห็นเวอร์ชัน (เช่น java version "17.0.1")** = ติดตั้งแล้ว ✅  
**ถ้าขึ้น "command not found" หรือ "not recognized"** = ยังไม่ได้ติดตั้ง ❌

#### วิธีติดตั้ง JDK
1. ดาวน์โหลด JDK จาก:
   - **Oracle JDK**: https://www.oracle.com/java/technologies/downloads/
   - **OpenJDK**: https://adoptium.net/ (แนะนำ - ฟรี)
   
2. เลือกเวอร์ชัน: **JDK 17** หรือ **JDK 21**

3. ติดตั้งตามขั้นตอน (Next > Next > Install)

4. **ตั้งค่า Environment Variables (สำคัญ!)**
   
   **Windows:**
   - กด Windows Key + R → พิมพ์ `sysdm.cpl` → Enter
   - ไปที่แท็บ "Advanced" → คลิก "Environment Variables"
   - ใน "System variables" → คลิก "New"
     - Variable name: `JAVA_HOME`
     - Variable value: `C:\Program Files\Java\jdk-17` (หรือพาธที่ติดตั้ง JDK)
   - เลือก "Path" → คลิก "Edit" → "New" → เพิ่ม `%JAVA_HOME%\bin`
   - คลิก OK ทุกหน้าต่าง

   **ทดสอบ:** เปิด CMD/PowerShell ใหม่ → พิมพ์ `java -version`

### 2. ✅ JSON Library (มีให้แล้วในโปรเจกต์)
- ไฟล์ `json-20231013.jar` อยู่ในโฟลเดอร์โปรเจกต์แล้ว
- **ไม่ต้องติดตั้งเพิ่ม** ✅

### 3. ✅ Supabase Account (สำหรับฐานข้อมูล)
1. สมัครฟรีที่: https://supabase.com
2. สร้างโปรเจกต์ใหม่
3. ไปที่ Settings > API → คัดลอก:
   - Project URL
   - anon/public key

4. อัปเดตใน `SupabaseConfig.java`:
```java
private static final String SUPABASE_URL = "YOUR_PROJECT_URL";
private static final String SUPABASE_KEY = "YOUR_ANON_KEY";
```

### 4. ✅ สร้างตารางฐานข้อมูล
ไปที่ Supabase → SQL Editor → รันโค้ดนี้:

```sql
-- ตาราง users
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  username VARCHAR(50) UNIQUE NOT NULL,
  password VARCHAR(100) NOT NULL,
  gender VARCHAR(10),
  birth_date DATE,
  role VARCHAR(20) DEFAULT 'user',
  created_at TIMESTAMP DEFAULT NOW()
);

-- ตาราง concerts
CREATE TABLE concerts (
  id SERIAL PRIMARY KEY,
  name VARCHAR(200) NOT NULL,
  artist VARCHAR(200),
  date DATE,
  time VARCHAR(50),
  location VARCHAR(200),
  description TEXT,
  image_url TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- ตาราง bookings
CREATE TABLE bookings (
  id SERIAL PRIMARY KEY,
  user_id INTEGER REFERENCES users(id),
  concert_id INTEGER REFERENCES concerts(id),
  seats TEXT,
  zone VARCHAR(50),
  total_price DECIMAL(10,2),
  status VARCHAR(20) DEFAULT 'confirmed',
  booking_date TIMESTAMP DEFAULT NOW()
);

-- สร้าง admin user
INSERT INTO users (username, password, role) 
VALUES ('admin', 'admin123', 'admin');
```

### 5. ✅ ตั้งค่า Storage Bucket (สำหรับรูปภาพคอนเสิร์ต)
1. ไปที่ Supabase → Storage
2. สร้าง bucket ใหม่ชื่อ `concert-images`
3. ตั้งเป็น **Public** bucket
4. ใน Policies → New Policy:
   - Policy name: "Public Access"
   - For: SELECT
   - Check: `true`
   - Save

### 6. ✅ ไฟล์ Resource ที่จำเป็น
- โฟลเดอร์ `file/` → ต้องมี `logo.png`
- ถ้าไม่มี ให้เตรียมโลโก้รูปใดรูปหนึ่งมาแล้วเปลี่ยนชื่อเป็น `logo.png`

---

## 🚀 วิธีรันโปรแกรม

### ขั้นตอนที่ 1: Compile
```bash
compile.bat
```
หรือ
```bash
javac -d bin -cp ".;json-20231013.jar" *.java
```

### ขั้นตอนที่ 2: Run
```bash
run.bat
```
หรือ
```bash
java -cp "bin;json-20231013.jar" Login
```

---

## 🔧 การแก้ปัญหาที่พบบ่อย

### ❌ ปัญหา: `'javac' is not recognized`
**สาเหตุ:** ไม่ได้ติดตั้ง JDK หรือไม่ได้ตั้งค่า PATH  
**วิธีแก้:**
1. ติดตั้ง JDK ตามขั้นตอนข้างต้น
2. ตั้งค่า JAVA_HOME และ PATH
3. เปิด CMD/PowerShell ใหม่
4. ทดสอบ: `java -version`

### ❌ ปัญหา: `Error: Could not find or load main class Login`
**สาเหตุ:** compile ไม่สำเร็จหรือไม่พบ class file  
**วิธีแก้:**
1. ลบโฟลเดอร์ `bin/` ทั้งหมด
2. รัน `compile.bat` ใหม่
3. ตรวจสอบว่าไม่มี error

### ❌ ปัญหา: `ClassNotFoundException: org.json.JSONObject`
**สาเหตุ:** ไม่พบไฟล์ json-20231013.jar  
**วิธีแก้:**
1. ตรวจสอบว่ามีไฟล์ `json-20231013.jar` ในโฟลเดอร์โปรเจกต์
2. รัน compile ด้วย classpath ที่ถูกต้อง:
   ```bash
   javac -d bin -cp ".;json-20231013.jar" *.java
   ```

### ❌ ปัญหา: รูปภาพโหลดไม่ขึ้น
**สาเหตุ:** ไม่มีไฟล์ logo.png  
**วิธีแก้:**
1. สร้างโฟลเดอร์ `file/` ถ้ายังไม่มี
2. ใส่ไฟล์รูป PNG ชื่อ `logo.png` ลงไป

### ❌ ปัญหา: เชื่อมต่อ Supabase ไม่ได้
**สาเหตุ:** ตั้งค่า URL/Key ผิด หรือไม่มีอินเทอร์เน็ต  
**วิธีแก้:**
1. ตรวจสอบ SUPABASE_URL และ SUPABASE_KEY ใน `SupabaseConfig.java`
2. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
3. ตรวจสอบว่าสร้างตารางในฐานข้อมูลแล้ว

### ❌ ปัญหา: Windows เตือน "Windows protected your PC"
**สาเหตุ:** Java application ยังไม่ได้ sign  
**วิธีแก้:**
1. คลิก "More info"
2. คลิก "Run anyway"

---

## 📋 Checklist ก่อนรัน

- [ ] ติดตั้ง JDK 17+ แล้ว
- [ ] ตั้งค่า JAVA_HOME และ PATH แล้ว
- [ ] มีไฟล์ json-20231013.jar
- [ ] สร้าง Supabase project แล้ว
- [ ] อัปเดต URL และ Key ใน SupabaseConfig.java แล้ว
- [ ] สร้างตารางฐานข้อมูลแล้ว
- [ ] สร้าง Storage bucket แล้ว
- [ ] มีไฟล์ logo.png ในโฟลเดอร์ file/
- [ ] มีอินเทอร์เน็ต

---

## 🎯 Quick Start สำหรับเครื่องใหม่

```bash
# 1. ติดตั้ง JDK จาก https://adoptium.net/
# 2. Clone หรือ download โปรเจกต์
# 3. ตั้งค่า Supabase (5 นาที)
# 4. อัปเดต SupabaseConfig.java
# 5. เตรียมไฟล์ logo.png
# 6. รัน:

compile.bat
run.bat
```

---

## 💡 Tips

1. **ใช้ VS Code/Eclipse/IntelliJ** สะดวกกว่า compile เอง
2. **Admin account**: username=`admin`, password=`admin123`
3. **เปลี่ยนรหัส admin** ใน Supabase หลังติดตั้งเสร็จ
4. **Test ก่อน deploy**: ทดสอบทุกฟีเจอร์ให้ทำงานได้ก่อน

---

## 📞 ติดปัญหา?

ถ้ายังติดปัญหา ให้ส่ง error message มาได้เลย!
